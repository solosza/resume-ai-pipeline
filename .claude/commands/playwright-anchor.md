# /playwright-anchor

Re-read Playwright protocol before test work.

## Instructions

Invoke `/kernel/anchor` with domain `playwright`.

This reads `docs/protocols/playwright-protocol.md` and updates state.
